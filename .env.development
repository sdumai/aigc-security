# Local dev calls the local Node proxy by default, so Volc APIs can be reached from localhost.
VITE_USE_MOCKS=false

# Vite proxies /api requests to this local backend. Start it with npm run dev or npm run dev:proxy.
VITE_DEV_API_PROXY_TARGET=http://localhost:3001
