# 与 deploy-backend.exp 配套：在服务器 venv 内安装（需先按机器安装 torch，见注释）
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
# pip install -r requirements.txt

diffusers>=0.27.0
transformers>=4.36.0
accelerate>=0.25.0
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pillow>=10.0.0
safetensors>=0.4.0
